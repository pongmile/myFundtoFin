# 🔧 แก้ไขปัญหา: ค่าใน Dashboard ไม่ตรงกับประวัติ Wealth

## 🐛 ปัญหาที่พบ

**อาการ:**
- หน้าแรก (Dashboard) แสดงค่า:
  - Cash: ฿1,313,020.28
  - Crypto: ฿647,173.80
  - Stock&Fund: ฿442,783.22
  
- แต่ในตาราง ประวัติ Wealth แสดงค่าที่ไม่ตรงกัน:
  - Cash: ฿1,226,172.00
  - Crypto: ฿163,158.29
  - Stock&Fund: ฿370,234.68

## 🔍 สาเหตุ

**Dashboard** คำนวณจาก:
- ✅ Cash → ใช้ยอดจริง + แปลงสกุลเงิน
- ✅ Crypto → ใช้ราคา Real-time จาก Bitkub/CoinGecko
- ✅ Stock&Fund → ใช้ราคา Real-time จาก Yahoo/SCBAM/Fund APIs

**Wealth History Save API (เดิม)** คำนวณจาก:
- ❌ Cash → ใช้ยอดจริง (ถูกต้อง)
- ❌ Crypto → ใช้ Cost Basis (ทุนต้น) ไม่ใช่มูลค่าตลาดจริง
- ❌ Stock&Fund → ใช้ Cost Basis (ทุนต้น) ไม่ใช่มูลค่าตลาดจริง

**ผลลัพธ์:** ค่าใน Wealth History ไม่ตรงกับ Dashboard เพราะใช้ Cost Basis แทนที่จะเป็นมูลค่าตลาด

---

## ✅ การแก้ไข

### 1. สร้างฟังก์ชันคำนวณมูลค่า Real-time

สร้างไฟล์ใหม่: [lib/portfolio-calculator.ts](../lib/portfolio-calculator.ts)

**ฟังก์ชันนี้จะ:**
- คำนวณCashพร้อมแปลงสกุลเงิน
- ดึงราคา Crypto แบบ Real-time
- ดึงราคาหุ้นและกองทุนแบบ Real-time
- คืนค่ามูลค่าตลาดจริงทั้งหมด

### 2. อัพเดท Save API

แก้ไข: [app/api/wealth-history/save/route.ts](../app/api/wealth-history/save/route.ts)

**เปลี่ยนจาก:**
```typescript
// ❌ เดิม - ใช้ cost_basis
const cryptoTotal = data.reduce((sum, item) => sum + item.cost_basis, 0);
const stocksTotal = data.reduce((sum, item) => sum + item.cost_basis, 0);
```

**เป็น:**
```typescript
// ✅ ใหม่ - คำนวณมูลค่าตลาดจริง
const { cashTotal, cryptoTotal, stocksTotal } = 
  await calculatePortfolioValues(supabaseUrl, supabaseServiceKey);
```

### 3. อัพเดท Cron API

แก้ไข: [app/api/wealth-history/cron/route.ts](../app/api/wealth-history/cron/route.ts)

ใช้ฟังก์ชันเดียวกัน เพื่อให้ Cron บันทึกค่าที่ถูกต้อง

---

## 🧪 วิธีทดสอบ

### 1. ลบข้อมูลวันนี้ออกก่อน (ถ้ามี)

```sql
-- ใน Supabase SQL Editor
DELETE FROM wealth_history WHERE date = '2026-01-29';
```

### 2. เริ่ม Dev Server

```bash
npm run dev
```

### 3. บันทึกใหม่ด้วย API

```powershell
# ใน PowerShell
Invoke-WebRequest -Uri "http://localhost:3000/api/wealth-history/save" -Method POST -UseBasicParsing
```

### 4. ตรวจสอบผลลัพธ์

เปิดหน้าเว็บและดู:
1. **Dashboard** → ดูค่าปัจจุบัน
2. **ประวัติ Wealth** → ควรเห็นวันที่ 29 ม.ค. 2569 ที่มีค่าตรงกับ Dashboard

---

## 📊 ตัวอย่างผลลัพธ์ที่ถูกต้อง

| แหล่งข้อมูล | Cash | Crypto | Stock&Fund | รวม |
|------------|--------|--------|---------------|-----|
| **Dashboard (Real-time)** | ฿1,313,020 | ฿647,174 | ฿442,783 | ฿2,402,977 |
| **ประวัติ Wealth (บันทึก)** | ฿1,313,020 | ฿647,174 | ฿442,783 | ฿2,402,977 |

**ควรตรงกัน!** ✅

---

## 🚀 สำหรับ Production (Vercel)

หลังจาก deploy บน Vercel:

1. **ค่าจะอัพเดทถูกต้องอัตโนมัติ** ตั้งแต่วันถัดไป
2. Cron จะบันทึกค่า Real-time ทุกวันเที่ยงคืน
3. ข้อมูลเก่าที่บันทึกไว้ยังเป็นค่าเดิม (Cost Basis)

**ถ้าต้องการแก้ข้อมูลย้อนหลัง:**
- ต้องรัน manual script เพื่อคำนวณใหม่
- หรือปล่อยให้ข้อมูลใหม่ถูกบันทึกทับทีละวัน

---

## 📝 สรุป

### ✅ สิ่งที่แก้ไขแล้ว

1. สร้างฟังก์ชัน `calculatePortfolioValues()` ที่คำนวณมูลค่าจริง
2. อัพเดท Save API ให้ใช้ฟังก์ชันใหม่
3. อัพเดท Cron API ให้ใช้ฟังก์ชันใหม่
4. ตอนนี้ Wealth History จะบันทึกมูลค่าตลาดจริง เหมือน Dashboard

### 🎯 ผลลัพธ์

- ✅ Dashboard และประวัติ Wealth แสดงค่าตรงกัน
- ✅ บันทึกมูลค่าตลาดจริง ไม่ใช่ Cost Basis
- ✅ Cron จะบันทึกค่าที่ถูกต้องทุกวันอัตโนมัติ

### 📌 หมายเหตุ

**ข้อมูลเก่าที่บันทึกไว้แล้ว (28 เม.ย. - 28 ม.ค.):**
- ยังเป็นค่า Cost Basis เดิม
- ไม่ได้แก้ไข (เพราะเป็นข้อมูลประวัติศาสตร์)
- ข้อมูลใหม่ตั้งแต่วันนี้เป็นต้นไปจะถูกต้อง

---

## 🔄 Next Steps

1. ทดสอบ API ตาม"วิธีทดสอบ" ข้างต้น
2. Deploy ขึ้น Vercel
3. ตรวจสอบว่า Cron ทำงานวันพรุ่งนี้ (30 ม.ค.)
4. เทียบค่าใน Dashboard กับประวัติ Wealth ว่าตรงกัน
